---
description: Show the maintainability dashboard (pending, recently resolved, rolling, suggested batches)
---

Invoke the maintainability skill in **list** mode. Read-only — no file writes.

See "Mode : list" in the skill's `SKILL.md`.
