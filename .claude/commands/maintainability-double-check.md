---
description: Deep-dive verification on a specific finding by ID (blast radius, feasibility, refined recommendation, verdict)
argument-hint: "<ID>"
---

Invoke the maintainability skill in **double-check** mode for finding ID `$ARGUMENTS`.

See "Mode : double-check" in the skill's `SKILL.md`.
