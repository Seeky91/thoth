---
description: Re-verify all pending findings against the current codebase and update their status
---

Invoke the maintainability skill in **update** mode.

See "Mode : update" in the skill's `SKILL.md`.
